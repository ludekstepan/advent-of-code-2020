import { stage1, stage2 } from './index'

describe('Day 5', () => {
    test('Stage 1', () => {
        expect(stage1()).toBe(undefined)
    })

    test('Stage 2', () => {
        expect(stage2()).toBe(undefined)
    })
})
